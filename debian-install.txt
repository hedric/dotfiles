-- Fix "user not in the sudoers file"

	- su root
	- apt-get install sudo -y
	- adduser username

* set correct permissions for sudoers file by inputting
	- chmod 0440 /etc/sudoers

* exit and shutdown system completely
* open term and try sudo echo "Hello Linux World!"

-- Fix resoltion (in this case 4K res not showing in Vbox)
	- xrandr --newmode "2560x1440_60.00" 311.83  2560 2744 3024 3488  1440 1441 1444 1490  -HSync +VSync
	- xrandr --addmode Virtual1 2560x1440_60.00

-- Install Virtualbox Guest Additions (actually fixes the resolution stuff)
	- sudo apt install linux-headers-$(uname -r) build-essential dkms
	
	- "Devices -> Insert Guest Additions CD Image"
	- Copy the files from mounted image to say Desktop
	
	- chmod +x VboxLinuxAdditions.run
	- sh ./VboxLinuxAdditions.run
	- reboot
	- DONE!
	
-- Fix so that apt-get will install stuff from the internet isntead of wanting a CD!!
	- Remove line with cdrom from /etc/apt/sources.list
	- EnFuckingJoy!
	- Update the package cache accordingly
	- sudo apt-get update

-- Remove KDE visual effects ...

	- System settings -> Hardware -> Compositer -> Enable compositer on startup [OFF]
	
-- Install i3 window manager
	- sudo apt-get install i3
	- logout -> session (change from plasma to i3 obviously)

-- Install feh (image viewer)
-- Install ranger (file manager)

-- Install urxvt (terminal emulator)
	- sudo apt-get install rxvt-unicode

